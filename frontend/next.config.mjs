/** @type {import('next').NextConfig} */
const nextConfig = {
  // Frontend deploys via Firebase App Hosting (managed Cloud Run, SSR).
  // See docs/runbooks/frontend-deploy.md for the one-time setup steps.
};

export default nextConfig;
